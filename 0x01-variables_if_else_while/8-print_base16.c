#include <stdio.h>

/**
 * main - Entry point
 * autor: salihu
 * Return: Always 0 (Success)
 */
int main(void)
{
	int salihu;
	char abdul;

	for (salihu = 48; salihu < 58; salihu++)
		putchar(salihu);
	for (abdul = 'a'; abdul <= 'f'; abdul++)
		putchar(abdul);
	putchar('\n');
	return (0);
}
