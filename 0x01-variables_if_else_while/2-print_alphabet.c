#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char salihu;

	for (salihu = 'a'; salihu <= 'z'; salihu++)
	{
		putchar(salihu);
	}
	putchar('\n');
	return (0);
}
